<h1>Users Index Page</h1>
<br>

<% @users.each do |user| %>
    <div class="col-sm-12">
    <!--VALUES-->
    <%= user.id %>.
    <%= user.email %>
    <br>

    <!-- LINKS -->
    <%= link_to 'Show', user_path(user.id) %>
    <%= link_to 'Edit', user_path(user.id)+"/edit" %>
    <%= link_to 'Destroy',  { action: :destroy, id: user.id }, method: :delete %>
    </div>
<% end  %>

<%= link_to 'new', new_user_path %>